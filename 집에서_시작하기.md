# 집에서 이어서 작업하기

## 0. 지금 상태 (2026-08-29 확인 완료)

| 항목 | 상태 |
|---|---|
| 사이트 배포 | ✅ https://iparksafetyinfo.github.io/safety-board/ |
| 첫 로그인 · 관리자 등록 | ✅ 완료 (`own` 권한 확보) |
| 보안 잠금 | ✅ 제3자 관리자 등록·데이터 접근 전부 차단 확인 |
| Firebase 연결 | ✅ 정상 |

**이제 바로 실무에 쓸 수 있는 상태입니다.**

---

## 1. 집 컴퓨터에서 5분 세팅

### 앱만 쓸 거면 — 설치 필요 없음
브라우저에서 주소 열고 로그인하면 끝입니다.
https://iparksafetyinfo.github.io/safety-board/

### 개발도 이어서 할 거면

1. **Claude 데스크톱 앱 설치** — claude.ai/download
2. **회사에서 쓰던 같은 계정으로 로그인**
3. 왼쪽 대화 목록에서 **이 작업 클릭** → 지금까지 맥락 그대로 이어짐
4. 집 컴퓨터 파일을 만지게 하려면 → **"이 컴퓨터에 연결"** 선택
   (선택지가 안 보이면 새 작업을 시작하고 아래 소스를 첨부)

### 소스 받기
https://github.com/iparksafetyinfo/safety-board
→ 초록색 **Code** 버튼 → **Download ZIP**

---

## 2. 혼자서 수정하는 법 (제 도움 없이)

GitHub에서 직접 고치면 1~2분 뒤 사이트에 자동 반영됩니다.

1. https://github.com/iparksafetyinfo/safety-board
2. **field.config.js** 클릭
3. 오른쪽 위 **연필 아이콘**
4. 수정
5. 아래 **Commit changes** 버튼

> `field.config.js` 외의 파일은 건드리지 마세요.
> 특히 `board.js` 를 고치면 다음 현장에서 재사용이 깨집니다.

---

## 3. 지금 남은 예시값 (실제 값으로 교체 필요)

`field.config.js` 에서:

```js
vendors: ["○○건설", "△△산업", "□□중기", "◇◇전기"],   // ← 실제 협력업체명

roles: {
  lead:  ["작업지휘자A", "작업지휘자B"],    // ← 실제 이름
  super: ["관리감독자A", "관리감독자B"],
  hse:   ["안전관리자A", "안전관리자B"]
},

geo: { lat: 37.7590, lon: 126.7770 },   // ← 현장 정확한 좌표
```

**현장 좌표 찾는 법**: 구글맵에서 현장 위치 우클릭 → 맨 위 숫자 두 개가 위도·경도입니다.

점검항목(`checks`)도 현장 실정에 맞는지 한번 봐주세요.

---

## 4. 팀원 추가

1. https://console.firebase.google.com/project/hdc-safety-board-pmc
2. **Authentication → 사용자 → 사용자 추가** (이메일·비밀번호 발급)
3. 생성된 **UID 복사**
4. **Realtime Database → 데이터** → `field / PMC-01 / members` 에서
   `+` 눌러 이름=UID, 값=등급 입력

| 등급 | 권한 |
|---|---|
| `own` | 전권 (등급 부여, 현장도 위치지정 포함) |
| `edit` | 작업 등록, 본인 것만 수정·삭제 |
| `view` | 조회·엑셀만 |

**members에 없으면 로그인해도 아무것도 안 보입니다.** (의도된 동작)

---

## 5. 계정 요약

| 항목 | 값 |
|---|---|
| 사이트 | https://iparksafetyinfo.github.io/safety-board/ |
| GitHub | iparksafetyinfo / safety-board |
| Firebase 프로젝트 | hdc-safety-board-pmc |
| 현장코드 | PMC-01 |
| 내 권한 | own (관리자) |

비밀번호는 여기 적지 마세요.

---

## 6. 다음에 물어보실 만한 것

집에서 대화 이어서 열고 그냥 말씀하시면 됩니다.

- "협력업체 명단 이걸로 바꿔줘" + 명단
- "현장 좌표 이거야" + 좌표
- "점검항목에 ○○ 추가해줘"
- "○○ 경보 기준을 △△로 바꿔줘"
- "다음 현장(○○)용으로 하나 더 만들어줘"

제가 수정해서 GitHub에 반영까지 하겠습니다.
