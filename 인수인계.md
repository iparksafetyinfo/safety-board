# 현장 상황판 — 인수인계

## 접속 주소

**https://iparksafetyinfo.github.io/safety-board/**

휴대폰에서 열고 → 공유 → **홈 화면에 추가** 하면 앱처럼 씁니다.
사무실 모니터는 **F 키**로 전체화면.

---

## 첫 로그인 (중요 — 처음 한 번만)

만들어두신 Firebase 계정으로 접속하세요.
**첫 접속자가 자동으로 관리자(own)가 됩니다.** 다른 사람보다 먼저 들어가세요.

> 이미 다른 사람이 먼저 들어가면 그 사람이 관리자가 됩니다.
> 그럴 땐 Firebase 콘솔에서 직접 고쳐야 하니, **가장 먼저 접속**해 주세요.

---

## 계정 정보

| 항목 | 값 |
|---|---|
| 사이트 | https://iparksafetyinfo.github.io/safety-board/ |
| GitHub 계정 | iparksafetyinfo |
| GitHub 저장소 | https://github.com/iparksafetyinfo/safety-board |
| Firebase 프로젝트 | hdc-safety-board-pmc |
| Firebase 콘솔 | https://console.firebase.google.com/project/hdc-safety-board-pmc |
| 현장코드 | PMC-01 |

비밀번호는 직접 관리하세요. (이 문서에 적지 마세요)

---

## 팀원 추가하는 법

1. Firebase 콘솔 → **Authentication → 사용자 → 사용자 추가** (이메일·비밀번호 발급)
2. 목록에서 그 사람의 **사용자 UID** 복사
3. **Realtime Database → 데이터** 에서 `field / PMC-01 / members` 아래에
   UID를 이름으로, 값을 등급으로 추가

| 등급 | 권한 |
|---|---|
| `own` | 전체 수정·삭제, 팀원 등급 부여, 현장도 위치지정 |
| `edit` | 작업 등록, 본인이 등록한 것만 수정·삭제 |
| `view` | 조회·엑셀 출력만 |

**members에 없는 계정은 로그인해도 아무것도 못 봅니다.**

---

## 집 컴퓨터에서 이어서 작업하기

### 1. Claude 데스크톱 앱 설치
claude.ai/download 에서 받아 설치 → **회사에서 쓰던 같은 계정으로 로그인**

### 2. 이 대화 이어서 열기
앱 왼쪽 대화 목록에 지금 이 작업이 그대로 있습니다. 클릭하면 이어집니다.
지금까지의 모든 맥락(도면, 설정, 결정사항)이 유지됩니다.

### 3. 집 컴퓨터 폴더 연결
대화창 하단 **폴더 추가** 버튼 → 작업할 폴더 선택
(회사 PC의 Desktop·Downloads 연결은 회사 PC 전용이라 집에선 다시 해야 합니다)

### 4. 파일 받기
집 컴퓨터엔 소스가 없으니 둘 중 하나:

- **간단**: 대화에 첨부된 `현장상황판_파주메디컬.zip` 다운로드
- **정석**: GitHub에서 받기 → https://github.com/iparksafetyinfo/safety-board
  초록색 **Code** 버튼 → **Download ZIP**

### 5. 수정한 내용 반영
GitHub 저장소 → 해당 파일 클릭 → 연필 아이콘 → 수정 → **Commit changes**
1~2분 뒤 사이트에 자동 반영됩니다.

파일을 통째로 바꾸려면 **Add file → Upload files** 로 같은 이름 파일을 올리면 덮어써집니다.

---

## 다음에 할 일

1. **첫 로그인** — 관리자 자동 등록 (제일 먼저)
2. **협력업체·담당자 명단 교체** — `field.config.js` 의 `vendors`, `roles`
   지금은 `○○건설`, `작업지휘자A` 같은 예시값입니다
3. **현장 좌표 확인** — `geo` 값이 파주 대략 위치라 날씨가 부정확할 수 있습니다
   구글맵에서 현장 우클릭 → 좌표 복사 → 교체
4. **점검항목 검토** — `checks` 가 현장 실정에 맞는지
5. 실제 작업 몇 건 등록해보고 경보가 맞게 뜨는지 확인

---

## 파일 어디에 뭐가 있나

| 파일 | 역할 | 수정 |
|---|---|---|
| `field.config.js` | **현장 설정 전부** | ← 여기만 고치면 됩니다 |
| `index.html` | 화면 구조 | 건드리지 마세요 |
| `board.js` | 로직·경보엔진 | 건드리지 마세요 |
| `board.css` | 디자인 | 건드리지 마세요 |
| `rules.json` | Firebase 보안규칙 | 콘솔에 붙여넣는 용도 |
| `plan.jpg` | 설계도면 현장도 | 교체 가능 |
| `site.jpg` | 항공사진 현장도 | 교체 가능 |

**`board.js`를 고치기 시작하면 다음 현장에서 재사용이 깨집니다.**
기능을 바꾸고 싶으면 `field.config.js` 로 뺄 수 있는지 먼저 검토하세요.

---

## 다음 현장으로 옮길 때 (5분)

1. GitHub에서 저장소 복사 (Fork 또는 새로 만들어 파일 업로드)
2. `field.config.js` 에서 **`field.code` 를 새 값으로** (제일 중요 — 이 값으로 데이터가 갈립니다)
3. `zones`, `levels`, `vendors`, `roles` 를 새 현장에 맞게
4. 도면·항공사진 교체
5. Firebase 콘솔에서 `field/{새현장코드}/members/` 에 인원 추가

**Firebase 설정은 그대로 두세요.** 프로젝트 하나가 여러 현장을 나눠 담습니다.

아파트 현장이면 `labels` 를 `{zone:"동", level:"층"}` 으로 바꾸고
`alerts.adjacentLevel` 을 `true` 로 켜면 상하층 낙하물 경보가 작동합니다.
