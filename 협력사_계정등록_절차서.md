# 협력사 계정 등록 절차서

파주메디컬클러스터 부지조성공사 · 현장 상황판 (PMC-01)

협력사 1곳을 등록하는 데 **2단계, 5분**이면 끝납니다. 계약 시점에 한 번만 하면 됩니다.

---

## 준비 — 먼저 확인할 것

`field.config.js` 의 `vendors` 목록에 그 업체 이름이 있어야 합니다. 없으면 먼저 추가하고 GitHub에 올리세요.

```js
vendors: ["동진토건"],          ← 신규 계약 시 여기에 추가
```

> **이름은 글자까지 똑같아야 합니다.** `동진토건` 과 `동진토건㈜` 는 다른 업체로 인식돼서
> 제출현황판에 "미제출"로 계속 남습니다. 띄어쓰기도 주의하세요.

---

## 1단계 · 계정 만들기

**Firebase Console → Authentication → Users → 사용자 추가**

https://console.firebase.google.com/project/hdc-safety-board-pmc/authentication/users

| 칸 | 넣을 값 | 비고 |
|---|---|---|
| 이메일 | `dongjin@pmc-01.kr` | **실제로 존재하는 메일 주소가 아니어도 됩니다.** 로그인 아이디 역할만 합니다 |
| 비밀번호 | 임시 비밀번호 (6자 이상) | 협력사에 전달할 값 |

만들면 목록에 한 줄 생깁니다. 그 줄 오른쪽의 **사용자 UID를 복사**하세요.
`YbrMEhVS3CSebl0zkXMcnxlyEzn1` 같은 28자리 문자열입니다.

> 이메일은 `업체약칭@pmc-01.kr` 처럼 규칙을 정해두면 나중에 헷갈리지 않습니다.

---

## 2단계 · 권한과 업체 지정

### 방법 A — 앱에서 (권장, 콘솔 안 들어가도 됨)

1단계에서 만든 이메일·비밀번호를 협력사에 알려주고 **한 번 로그인**하게 합니다.
그러면 원청 화면 **[인원]** 목록에 그 계정이 나타납니다.
거기서 **권한 = 협력사**, **업체 = 동진토건** 을 고르면 끝입니다. (바로 반영됩니다)

> 로그인만 해서는 아무 데이터도 못 봅니다. 권한을 주기 전까지는 빈 화면입니다.

### 방법 B — 콘솔에서 직접

**Firebase Console → Realtime Database → 데이터**

https://console.firebase.google.com/project/hdc-safety-board-pmc/database

`field` → `PMC-01` → `members` 로 내려가서, **`members` 오른쪽 `+`** 를 누릅니다.

- **이름** 칸: 1단계에서 복사한 **UID**
- **값** 칸: 비워두고, 다시 그 아래 `+` 를 눌러 아래 세 줄을 넣습니다

| 이름 | 값 |
|---|---|
| `rank` | `edit` |
| `vendor` | `동진토건` |
| `name` | `김소장` (선택) |

**추가** 를 누르면 끝입니다.

### 더 빠른 방법 — JSON 통째로 넣기

`members` 노드에서 오른쪽 **⋮ → JSON 가져오기** 를 쓰면 여러 업체를 한 번에 넣을 수 있습니다.
같은 폴더의 `members-양식.json` 을 열어 UID만 바꿔서 올리세요.

> ⚠️ **JSON 가져오기는 기존 내용을 통째로 덮어씁니다.** 반드시 지금 있는 계정(원청 `own` 포함)까지
> 전부 담긴 상태로 올리세요. 양식 파일에 사장님 계정이 이미 들어 있습니다.

---

## 3단계 · 협력사에 전달

이 세 가지만 알려주면 됩니다.

```
주소     : https://<계정>.github.io/<저장소>/
아이디   : dongjin@pmc-01.kr
비밀번호 : (1단계에서 정한 값)
```

전달할 때 같이 안내할 내용:

- **전날 17시까지** 익일 작업을 [작업투입] 에서 등록
- 업체 칸은 자동으로 고정돼 있으니 건드릴 필요 없음
- 등록 후 [기록] 에서 원청 승인 여부와 **반려 사유**를 확인
- 반려되면 내용을 고쳐서 다시 저장 (수정하면 검토 대기로 되돌아감)

---

## 등급 정리

| 등급 | 누구에게 | 할 수 있는 것 |
|---|---|---|
| `own` | 원청 안전관리자 | 전체 수정·삭제, **승인·반려**, 인원 등급 부여 |
| `edit` + `vendor` | 협력사 소장 | 자기 업체 작업만 등록·수정, 지정 업체로만 등록 가능 |
| `view` | 감리·본사 등 | 조회·엑셀만 |

**members 에 없는 계정은 로그인해도 데이터를 한 줄도 못 읽습니다.** 보안 규칙에 그렇게 걸려 있습니다.

---

## 사람이 바뀌었을 때

| 상황 | 할 일 |
|---|---|
| 협력사 소장 교체 | Authentication 에서 해당 계정 **비밀번호 재설정** → 새 소장에게 전달. UID는 그대로라 DB는 안 건드려도 됩니다 |
| 협력사 계약 종료 | `members/{UID}` 를 **삭제**. 계정은 남겨둬도 데이터를 못 봅니다. `vendors` 목록에서도 이름을 빼세요 |
| 업체명 변경 | `members/{UID}/vendor` 와 `field.config.js` 의 `vendors` **양쪽 다** 고쳐야 합니다 |

---

## 잘 안 될 때

| 증상 | 원인 |
|---|---|
| 로그인은 되는데 화면이 텅 빔 | `members` 에 UID가 안 들어갔거나 오타. 복사한 UID를 다시 확인 |
| 등록했는데 제출현황판에 "미제출" | `vendor` 값과 `vendors` 목록의 이름이 다름. 글자·띄어쓰기 대조 |
| 업체 칸이 잠기지 않고 선택 가능 | `rank` 가 `own` 으로 들어감. 협력사는 반드시 `edit` |
| 등록 버튼을 눌러도 저장 안 됨 | 필수 점검 3개(안전난간·TBM·보호구) 미확인 |
